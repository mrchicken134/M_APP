# M_APP

Tauri + React + TypeScript desktop application.

## Development

Install dependencies:

```powershell
npm.cmd install
```

Start the independent desktop window:

```powershell
npm.cmd run tauri dev
```

Build the application:

```powershell
npm.cmd run tauri build
```
